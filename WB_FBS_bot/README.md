# WB FBS Bot 📦

Telegram бот для мониторинга новых заказов FBS с Wildberries и автоматической отправки уведомлений. Построен на основе объектно-ориентированного программирования с модульной архитектурой.

## Возможности

* ✅ Автоматический мониторинг новых заказов FBS с Wildberries
* ✅ Уведомления в Telegram о новых заказах
* ✅ Предотвращение дублирования через SQLite базу данных
* ✅ Автоматическое получение chat_id через команду `/start`
* ✅ Модульная архитектура на основе ООП
* ✅ Настраиваемый интервал опроса (по умолчанию 3 минуты)
* ✅ Детальная информация о заказах в уведомлениях
* ✅ Ежедневная статистика по заказам в 00:00
* ✅ Ежедневный отчет о просмотрах карточек товаров в 00:05

## Технологии

**Python, requests, python-dotenv, SQLite**

## Быстрый старт

### Вариант 1: Использование скрипта запуска (рекомендуется)

```bash
# Создание .env файла
cp .env.example .env

# Заполните .env файл:
# WB_API_KEY=your_wildberries_api_key_here
# TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Запуск через скрипт (автоматически создаст и активирует виртуальную среду)
chmod +x start.sh
./start.sh
```

### Вариант 2: Ручная установка

```bash
# Создание виртуальной среды
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установка зависимостей
pip install -r requirements.txt

# Создание .env файла
cp .env.example .env

# Заполните .env файл:
# WB_API_KEY=your_wildberries_api_key_here
# TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Запуск приложения
python main.py
```

**Важно:** После запуска отправьте команду `/start` вашему боту в Telegram в течение 60 секунд. Chat ID будет автоматически сохранен в базе данных для последующих запусков.

## Основные команды

* `/start` - Активация бота и получение chat_id (необходимо при первом запуске)

## Использование

1. Запустите приложение: `python main.py`
2. Отправьте команду `/start` боту в Telegram (в течение 60 секунд после запуска)
3. Приложение начнет автоматически проверять новые заказы FBS каждые 3 минуты
4. Вы будете получать уведомления о новых заказах в Telegram с детальной информацией

## Особенности

* ✅ Модульная архитектура на основе ООП
* ✅ Автоматическое получение chat_id через команду `/start`
* ✅ Предотвращение дублирования уведомлений через SQLite
* ✅ Детальная информация о заказах в уведомлениях
* ✅ Настраиваемый интервал опроса
* ✅ Graceful shutdown для корректного завершения работы

## Архитектура

Приложение построено на основе объектно-ориентированного программирования и состоит из следующих модулей:

* **config/** - Конфигурация приложения (загрузка из .env)
* **database/** - Работа с базой данных SQLite (хранение обработанных заказов и настроек)
* **api/** - Клиент для работы с API Wildberries
* **telegram/** - Интеграция с Telegram ботом (отправка уведомлений)
* **monitor/** - Основная логика мониторинга заказов (оркестрация всех компонентов)

## Структура проекта

```
WB_FBS_bot/
├── config/
│   ├── __init__.py
│   └── config.py          # Конфигурация приложения
├── database/
│   ├── __init__.py
│   └── db_manager.py      # Менеджер базы данных SQLite
├── api/
│   ├── __init__.py
│   └── wb_client.py       # Клиент API Wildberries
├── telegram/
│   ├── __init__.py
│   └── bot.py             # Telegram бот
├── monitor/
│   ├── __init__.py
│   └── order_monitor.py   # Монитор заказов
├── main.py                # Точка входа
├── requirements.txt       # Зависимости
├── start.sh               # Скрипт запуска с виртуальной средой
├── .env.example           # Пример конфигурации
└── README.md              # Документация
```

## Настройка

Вы можете настроить следующие параметры в файле `.env`:

- `WB_API_KEY` - API ключ Wildberries (обязательно)
- `TELEGRAM_BOT_TOKEN` - Токен Telegram бота (обязательно)
- `TELEGRAM_CHAT_ID` - ID чата для уведомлений (опционально, будет получен автоматически при отправке `/start`)
- `WB_ANALYTICS_API_KEY` - API ключ для Analytics API (опционально, если отличается от WB_API_KEY)
- `WB_API_URL` - URL API (по умолчанию: https://marketplace-api.wildberries.ru/api/v3/orders/new)
- `WB_POLL_INTERVAL` - Интервал опроса в секундах (по умолчанию: 180)
- `DB_PATH` - Путь к файлу базы данных (по умолчанию: orders.db)

**Важно:** Chat ID автоматически сохраняется в базе данных после первого использования команды `/start`, поэтому его не нужно указывать в `.env` при последующих запусках.
