# 📅 Telegram Booking Bot

Масштабируемый Telegram бот для записи на услуги, рассчитанный на 5к+ клиентов. Построен на **aiogram 3.x** с использованием **модульной архитектуры** и **объектно-ориентированного программирования**.

[![Python](https://img.shields.io/badge/Python-3.10+-blue.svg)](https://www.python.org/)
[![aiogram](https://img.shields.io/badge/aiogram-3.x-green.svg)](https://docs.aiogram.dev/)
[![License](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)

## 🚀 Основные возможности

### Для клиентов:
- ✅ **Запись через inline-кнопки** - удобный интерфейс с календарем
- ✅ **Парсинг естественного языка** - запись через текстовые сообщения ("завтра 15:00")
- ✅ **Визуальное отображение времени** - занятые времена помечены ❌, прошедшие ⏰
- ✅ **Просмотр своих записей** - список всех записей с возможностью отмены
- ✅ **Автоматические напоминания** - уведомление за 30 минут до записи
- ✅ **Просмотр услуг** - информация о доступных услугах с длительностью и стоимостью
- ✅ **Автоматический возврат в меню** - после подтверждения записи

### Для администраторов:
- ✅ **Админ-панель** - полный контроль над ботом
- ✅ **Управление услугами** - добавление, редактирование, удаление (деактивация) услуг
- ✅ **Просмотр записей** - по датам, на сегодня, статистика
- ✅ **Управление записями** - подтверждение и отмена записей
- ✅ **Настройка времени работы** - начало и конец рабочего дня
- ✅ **Настройка интервала** - между записями (в минутах)
- ✅ **Управление администраторами** - добавление новых админов
- ✅ **Уведомления о новых записях** - автоматические с автоудалением через 5 секунд

## 📁 Структура проекта

```
booking-bot/
├── main.py                    # Точка входа, инициализация бота
├── src/                       # Модульная архитектура
│   ├── __init__.py
│   ├── config.py             # Класс Config (ООП, Pydantic Settings)
│   ├── bot/                  # Telegram bot логика
│   │   ├── __init__.py
│   │   ├── handlers.py      # Все обработчики команд и callback'ов
│   │   ├── keyboards.py     # Inline-кнопки, календарь, клавиатуры
│   │   └── states.py        # FSM состояния (BookingStates, AdminStates)
│   ├── database/            # Работа с БД
│   │   ├── __init__.py
│   │   └── models.py        # Класс Database (ООП, CRUD операции)
│   ├── services/            # Бизнес-логика
│   │   ├── __init__.py
│   │   ├── notifications.py # Уведомления администраторов
│   │   └── reminders.py     # Автоматические напоминания
│   └── utils/               # Утилиты
│       ├── __init__.py
│       ├── helpers.py       # Вспомогательные функции (статусы, эмодзи)
│       └── nlp.py           # Парсинг естественного языка (даты, время)
├── .env                      # Конфигурация (не коммитится)
├── .env.example              # Пример конфигурации
├── requirements.txt          # Зависимости Python
└── README.md                 # Документация
```

## 🛠 Установка

### 1. Клонируйте репозиторий или создайте проект

### 2. Установите зависимости:

**Windows (PowerShell):**
```powershell
py -m pip install -r requirements.txt
```

**Linux/Mac:**
```bash
pip install -r requirements.txt
```

### 3. Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

### 4. Заполните `.env`:

```env
# Telegram Bot Configuration
BOT_TOKEN=your_bot_token_here

# Admin Configuration
ADMIN_ID=your_admin_telegram_id

# Database Configuration
DB_PATH=booking_bot.db

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=bot.log

# Working Hours Configuration (0-23)
WORKING_HOURS_START=9
WORKING_HOURS_END=18

# Appointment Configuration (minutes)
APPOINTMENT_INTERVAL=30
```

### 5. Запустите бота:

**Windows:**
```powershell
py main.py
```

**Linux/Mac:**
```bash
python main.py
```

## 📸 Демонстрация

### Интерфейс для клиентов

#### Главное меню
![Главное меню](screenshots/client/01_main_menu.png)
*Главное меню с основными функциями бота*

#### Выбор услуги
![Список услуг](screenshots/client/02_services_list.png)
*Список доступных услуг с информацией о длительности и стоимости*

#### Календарь
![Календарь](screenshots/client/03_calendar.png)
*Интерактивный календарь для выбора даты записи*

#### Выбор времени
![Выбор времени](screenshots/client/04_time_selection.png)
*Выбор времени с визуальными индикаторами (доступные, занятые, прошедшие)*

#### Мои записи
![Мои записи](screenshots/client/07_my_appointments.png)
*Список всех записей пользователя с возможностью отмены*

### Админ-панель

#### Панель администратора
![Админ-панель](screenshots/admin/01_admin_panel.png)
*Главная панель администратора с основными функциями управления*

#### Добавление услуги
![Добавление услуги](screenshots/admin/02_add_service.png)
*Интерфейс добавления новой услуги*

#### Управление услугами
![Список услуг](screenshots/admin/03_services_list.png)
*Список всех услуг с возможностью редактирования и удаления*

#### Записи на сегодня
![Записи на сегодня](screenshots/admin/04_today_appointments.png)
*Просмотр всех записей на текущий день*

#### Статистика
![Статистика](screenshots/admin/08_statistics.png)
*Общая статистика по записям и услугам*

## 📝 Использование

### Для пользователей:

#### Команды:
- `/start` - Главное меню
- `/help` - Справка по использованию бота
- `/cancel` - Отменить текущее действие

#### Запись через меню:
1. Нажмите "📅 Записаться"
2. Выберите услугу из списка
3. Выберите дату в календаре
4. Выберите время (доступные времена без символов, занятые с ❌, прошедшие с ⏰)
5. Подтвердите запись

#### Быстрая запись через текст:
Бот понимает естественный язык:
- "завтра 15:00"
- "послезавтра в 10"
- "25.12 в 14:30"
- "сегодня в 16"

#### Просмотр записей:
- Нажмите "📋 Мои записи" для просмотра всех ваших записей
- Можно отменить запись через кнопку "❌ Отменить запись"

#### Напоминания:
- Автоматическое уведомление за 30 минут до записи
- Напоминания отправляются только один раз
- Уведомление содержит информацию об услуге, дате и времени

### Для администраторов:

#### Доступ к админ-панели:
- Кнопка "🔧 Админ-панель" в главном меню (только для администраторов)

#### Управление услугами:
1. **Добавить услугу:**
   - Нажмите "➕ Добавить услугу"
   - Введите название, длительность, стоимость
   - Описание можно пропустить командой `/skip`

2. **Редактировать услугу:**
   - Нажмите "📋 Список услуг"
   - Выберите услугу для редактирования
   - Измените нужные поля (название, длительность, стоимость, описание)
   - Можно активировать/деактивировать услугу

3. **Удалить услугу:**
   - Услуга деактивируется (мягкое удаление)
   - Не отображается в списке для клиентов
   - Можно восстановить через редактирование

#### Просмотр записей:
- **На сегодня:** "📅 Записи на сегодня"
- **По дате:** "📆 Записи по дате" - выбор через календарь
- **Статистика:** "📊 Статистика" - общая информация о записях

#### Управление записями:
- Подтверждение записи: кнопка "✅ Подтвердить #ID"
- Отмена записи: кнопка "❌ Отменить #ID"
- Просмотр деталей записи с информацией о клиенте

#### Настройки:
- **⚙️ Настройки** - доступ к настройкам бота:
  - 🕐 Время начала работы (0-23)
  - 🕐 Время окончания работы (0-23)
  - ⏱ Интервал между записями (15-480 минут)

#### Управление администраторами:
- Просмотр списка администраторов
- Добавление новых администраторов (через код)

## 🏗 Архитектура

### Модульная структура

Проект организован по принципу **модульной архитектуры** с разделением ответственности:

- **`src/config.py`** - Управление конфигурацией через Pydantic Settings
- **`src/database/models.py`** - Слой работы с базой данных (CRUD операции)
- **`src/bot/handlers.py`** - Все обработчики команд и callback'ов (Router-based)
- **`src/bot/keyboards.py`** - Генерация inline-клавиатур
- **`src/bot/states.py`** - FSM состояния для управления диалогами
- **`src/services/`** - Бизнес-логика (уведомления, напоминания)
- **`src/utils/`** - Вспомогательные функции (NLP, helpers)

### Dependency Injection

Бот использует **Dependency Injection** через middleware для передачи зависимостей (`db`, `config`, `bot`, `admin_id`) в обработчики, что обеспечивает:
- Лучшую тестируемость
- Разделение ответственности
- Упрощение поддержки

### Объектно-ориентированный подход

- **`Config`** - класс для управления конфигурацией с валидацией
- **`Database`** - класс для работы с БД с асинхронными методами
- Все сервисы организованы как модули с четкой ответственностью

## 🗄 База данных

### Структура таблиц:

#### `appointments` (записи):
- `id` - уникальный ID записи
- `client_id` - ID клиента в Telegram
- `client_name` - имя клиента
- `client_username` - username клиента
- `service_id` - ID услуги
- `date` - дата записи (YYYY-MM-DD)
- `time` - время записи (HH:MM)
- `status` - статус (pending/confirmed/cancelled)
- `notes` - примечания
- `reminder_sent` - флаг отправки напоминания (0/1)
- `created_at` - дата создания
- `updated_at` - дата обновления

#### `services` (услуги):
- `id` - уникальный ID услуги
- `name` - название услуги
- `duration` - длительность в минутах
- `price` - стоимость
- `description` - описание
- `is_active` - активна ли услуга (0/1)
- `created_at` - дата создания

#### `admins` (администраторы):
- `id` - уникальный ID
- `user_id` - ID администратора в Telegram
- `username` - username администратора
- `full_name` - полное имя
- `created_at` - дата добавления

#### `settings` (настройки):
- `key` - ключ настройки
- `value` - значение настройки
- `updated_at` - дата обновления

### Индексы для оптимизации:
- `idx_date_time` - по дате и времени записей
- `idx_status` - по статусу записей
- `idx_client_id` - по ID клиента
- `idx_service_id` - по ID услуги

## 🔧 Технические детали

### Технологии:
- **aiogram 3.x** - современный фреймворк для Telegram ботов
- **aiosqlite** - асинхронная работа с SQLite
- **Pydantic Settings** - управление конфигурацией с валидацией
- **FSM (Finite State Machine)** - управление состояниями диалога
- **python-dateutil** - парсинг дат из естественного языка

### Особенности реализации:

#### Визуальное отображение времени:
- **Доступные времена:** обычный текст (например, "10:00")
- **Занятые времена:** с крестиком (например, "❌ 10:00")
- **Прошедшие времена:** с часами (например, "⏰ 09:00")
- Занятые и прошедшие времена нельзя выбрать (показывается alert)

#### Умная проверка времени:
- Не предлагаются прошедшие времена на сегодняшнюю дату
- Учитывается длительность услуги при проверке пересечений
- Используются настройки времени работы из админ-панели

#### Автоматические напоминания:
- Фоновая задача проверяет записи каждую минуту
- Отправка напоминаний за 30 минут до записи
- Напоминания отправляются только один раз
- Только для записей со статусом "confirmed"

#### Уведомления администраторов:
- Автоматические уведомления о новых записях
- Автоудаление уведомлений через 5 секунд
- Отправка всем администраторам

#### Graceful Shutdown:
- Корректное закрытие соединений с БД
- Сохранение состояния при остановке

## 🚀 Деплой

### Локально (для тестирования):
```bash
python main.py
```

### VPS (Timeweb/Reg.ru и др.):

1. **Загрузите файлы на сервер** (через FTP/SFTP)

2. **Установите зависимости:**
```bash
pip3 install -r requirements.txt
```

3. **Настройте `.env` файл**

4. **Запустите через `screen` или `tmux`:**
```bash
screen -S booking-bot
python3 main.py
# Ctrl+A, затем D для отсоединения
```

### Автозапуск через systemd:

Создайте файл `/etc/systemd/system/booking-bot.service`:

```ini
[Unit]
Description=Telegram Booking Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/booking-bot
ExecStart=/usr/bin/python3 /path/to/booking-bot/main.py
Restart=always
RestartSec=10
Environment="PATH=/usr/bin:/usr/local/bin"

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl daemon-reload
sudo systemctl enable booking-bot
sudo systemctl start booking-bot
sudo systemctl status booking-bot
```

## 📊 Производительность

- **SQLite оптимален** для до 5к+ клиентов
- **Индексы ускоряют** поиск записей по дате, времени, статусу
- **Асинхронные операции** через aiosqlite для неблокирующей работы
- **Оптимизированные запросы** с использованием JOIN для получения связанных данных
- **Graceful shutdown** для корректного завершения работы
- **Модульная архитектура** упрощает масштабирование и поддержку

## 🔐 Безопасность

- Токен бота хранится в `.env` (не коммитится в Git)
- Проверка прав администратора перед выполнением админ-команд
- Валидация входных данных (даты, времени, чисел)
- Защита от SQL-инъекций через параметризованные запросы
- Валидация конфигурации через Pydantic Settings

## 📝 Логирование

Логи сохраняются в файл `bot.log` и выводятся в консоль:
- INFO - общая информация о работе бота
- WARNING - предупреждения (например, попытка выбрать недоступное время)
- ERROR - ошибки с детальной информацией

## 🐛 Решение проблем

### Бот не запускается:
1. Проверьте наличие `.env` файла
2. Убедитесь, что `BOT_TOKEN` и `ADMIN_ID` заполнены
3. Проверьте установку зависимостей: `pip list`
4. Проверьте валидацию конфигурации (ошибки выводятся при запуске)

### Ошибки с базой данных:
1. Удалите файл `booking_bot.db` (будет создан заново)
2. Проверьте права доступа к файлу БД

### Напоминания не отправляются:
1. Проверьте, что запись имеет статус "confirmed"
2. Убедитесь, что бот запущен (напоминания проверяются каждую минуту)
3. Проверьте логи на наличие ошибок

### Проблемы с импортами:
1. Убедитесь, что все зависимости установлены: `pip install -r requirements.txt`
2. Проверьте, что вы запускаете бота из корневой директории проекта
3. Убедитесь, что структура `src/` соответствует документации

## 🔄 Обновления

### Версия 2.0 (текущая):
- ✅ Модульная архитектура с разделением на `src/`
- ✅ ООП подход (Config, Database классы)
- ✅ Dependency Injection для обработчиков
- ✅ Router-based handlers (aiogram 3.x)
- ✅ Pydantic Settings для конфигурации
- ✅ Вынесение бизнес-логики в services
- ✅ Улучшенная структура проекта

### Версия 1.0:
- ✅ Базовая функциональность записи
- ✅ Админ-панель
- ✅ Управление услугами
- ✅ Настройка времени работы
- ✅ Автоматические напоминания
- ✅ Визуальное отображение занятых времен
- ✅ Парсинг естественного языка
- ✅ Статистика и отчеты

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в файле `bot.log`
2. Убедитесь, что все зависимости установлены
3. Проверьте настройки в `.env`
4. Убедитесь, что структура проекта соответствует документации

## 📝 Лицензия

MIT License

---


**Разработано с использованием:**
- [aiogram 3.x](https://docs.aiogram.dev/) - современный фреймворк для Telegram ботов
- [aiosqlite](https://aiosqlite.omnilib.dev/) - асинхронная работа с SQLite
- [Pydantic Settings](https://docs.pydantic.dev/latest/concepts/pydantic_settings/) - управление конфигурацией
- [python-dateutil](https://dateutil.readthedocs.io/) - парсинг дат
- [python-dotenv](https://github.com/theskumar/python-dotenv) - загрузка переменных окружения
