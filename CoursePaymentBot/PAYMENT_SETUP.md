# 🔧 Настройка платежей через ЮKassa для Telegram

## ❌ Проблема: PAYMENT_PROVIDER_INVALID

Ошибка возникает, потому что используется **API токен** вместо **токена провайдера для Telegram Payments**.

## ✅ Решение: Получение правильного токена

### Шаг 1: Войдите в личный кабинет ЮKassa
1. Перейдите на https://yookassa.ru/
2. Войдите в личный кабинет

### Шаг 2: Получите токен провайдера для Telegram
1. Перейдите в раздел **"Настройки"** → **"Интеграция"**
2. Найдите раздел **"Telegram"** или **"Telegram Payments"**
3. Скопируйте **токен провайдера** (Provider Token)

**Формат токена должен быть примерно таким:**
- Для теста: `381764678:TEST:XXXXX` 
- Для продакшена: `381764678:LIVE:XXXXX`

**НЕ используйте:**
- ❌ API токен (формат: `test_XXXXX` или `live_XXXXX`)
- ❌ Секретный ключ
- ❌ Shop ID отдельно

### Шаг 3: Обновите .env файл
Замените `PROVIDER_TOKEN` на полученный токен провайдера:

```env
PROVIDER_TOKEN=381764678:TEST:ваш_токен_здесь
```

### Шаг 4: Перезапустите бота

## 🧪 Тестирование платежей

После настройки правильного токена используйте тестовые карты:

### Тестовая карта ЮKassa:
- **Номер**: `4111 1111 1111 1111`
- **Срок**: `12/25` (любая будущая дата)
- **CVV**: `123` (любое 3-значное число)

### Альтернативные тестовые карты:
- `5555 5555 5555 4444` | `11/22` | `333`
- `4242 4242 4242 4242` | `12/25` | `123`

## 📝 Важные замечания

1. **Тестовый режим**: Убедитесь, что используете тестовый токен (`TEST`) для разработки
2. **Бот должен быть зарегистрирован**: Telegram должен знать о вашем боте для приема платежей
3. **Регистрация в Telegram**: Возможно, потребуется регистрация бота для приема платежей через @BotFather

## 🔍 Проверка токена

Если токен правильный, в логах вы увидите:
```
INFO - Invoice sent to user XXXXX
```

Если токен неправильный, будет ошибка:
```
ERROR - PAYMENT_PROVIDER_INVALID
```

## 📞 Если проблема сохраняется

1. Проверьте, что токен скопирован полностью (без пробелов)
2. Убедитесь, что используете токен провайдера, а не API токен
3. Проверьте, что бот зарегистрирован для приема платежей
4. Обратитесь в поддержку ЮKassa, если токен не работает

