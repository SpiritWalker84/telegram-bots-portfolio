# 🔑 Как получить токен провайдера после подключения через BotFather

После подключения платежей через BotFather, токен нужно получить в личном кабинете ЮKassa.

## 📍 Где найти токен:

### Вариант 1: В личном кабинете ЮKassa

1. **Войдите в личный кабинет ЮKassa**: https://yookassa.ru/
2. Перейдите в раздел **"Настройки"** (или **"Магазины"**)
3. Выберите ваш магазин (или создайте новый, если его нет)
4. Найдите раздел **"Интеграция"** или **"Telegram"**
5. Скопируйте **токен провайдера** (Provider Token)

**Формат токена должен быть:**
```
381764678:TEST:ваш_секретный_ключ_здесь
```
или
```
381764678:LIVE:ваш_секретный_ключ_здесь
```

### Вариант 2: Через API (если токен не виден в интерфейсе)

Если токен не отображается в интерфейсе, можно получить его через API:

1. В личном кабинете ЮKassa найдите:
   - **Shop ID** (идентификатор магазина, например: `381764678`)
   - **Секретный ключ** (Secret Key, формат: `test_XXXXX` или `live_XXXXX`)

2. Сформируйте токен провайдера в формате:
   ```
   {SHOP_ID}:TEST:{SECRET_KEY}
   ```
   
   Например, если:
   - Shop ID = `381764678`
   - Secret Key = `test_ABC123XYZ`
   
   То токен будет: `381764678:TEST:test_ABC123XYZ`

### Вариант 3: Проверьте историю сообщений с BotFather

Иногда BotFather отправляет токен в сообщении после подключения. Проверьте историю переписки с @BotFather.

## ⚠️ Важно:

- **НЕ используйте** просто API токен (`test_XXXXX`) - нужен полный формат с Shop ID
- Токен должен содержать двоеточия: `SHOP_ID:TEST:SECRET_KEY`
- Для тестирования используйте `TEST`, для продакшена - `LIVE`

## 🔍 Как проверить, что токен правильный:

1. Обновите `.env` файл с полученным токеном
2. Перезапустите бота
3. Попробуйте нажать "Купить курс"
4. Если токен правильный - откроется окно оплаты
5. Если неправильный - будет ошибка `PAYMENT_PROVIDER_INVALID`

## 📞 Если токен не найден:

1. Убедитесь, что магазин создан в ЮKassa
2. Проверьте, что подключение через BotFather завершено успешно
3. Обратитесь в поддержку ЮKassa: support@yookassa.ru

