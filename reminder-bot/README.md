# 🤖 Telegram Reminder Bot

Telegram-бот на Python с использованием **aiogram 3.x** и **SQLite** для управления задачами и напоминаниями.

## ✨ Возможности

- ✅ Добавление задач с напоминаниями
- ⏰ Гибкий парсинг времени (сегодня, завтра, конкретная дата)
- 📋 Просмотр списка задач
- ✔️ Отметка задач как выполненных
- 🗑️ Удаление задач
- ⚙️ Настройка автоудаления выполненных задач
- 🔔 Автоматическая отправка напоминаний

## 📁 Структура проекта

```
telegram-reminder-bot/
├── main.py              # основной файл бота
├── .env.example         # шаблон для токена
├── requirements.txt     # зависимости Python
├── start.bat            # скрипт запуска для Windows
└── README.md            # документация
```

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/SpiritWalker84/telegram-bots-portfolio.git
cd telegram-bots-portfolio/reminder-bot
```

### 2. Установка зависимостей

**Для Windows:**
```bash
py -m pip install -r requirements.txt
```

**Для Linux/Mac:**
```bash
pip install -r requirements.txt
```

### 3. Настройка бота

1. Создайте файл `.env` в корне проекта:
   ```env
   BOT_TOKEN=your_bot_token_here
   ```

2. Получите токен:
   - Откройте [@BotFather](https://t.me/BotFather) в Telegram
   - Отправьте команду `/newbot`
   - Следуйте инструкциям
   - Скопируйте токен в файл `.env`

### 4. Запуск

**Для Windows:**
```bash
start.bat
```
или
```bash
py main.py
```

**Для Linux/Mac:**
```bash
python main.py
```

## 📋 Команды бота

| Команда | Описание | Пример |
|---------|----------|--------|
| `/start` | Приветствие и инструкции | `/start` |
| `/add <текст> [время]` | Добавить задачу | `/add Купить молоко в 14:30` |
| `/list` | Показать все задачи | `/list` |
| `/done <id>` | Отметить задачу выполненной | `/done 1` |
| `/delete <id>` | Удалить задачу | `/delete 1` |
| `/settings` | Настройки автоудаления | `/settings` |

## 💡 Примеры использования

### Добавление задач

```bash
/add Купить молоко в 14:30              # Сегодня в 14:30
/add Встреча в 2025-12-26 15:00         # Конкретная дата
/add Оплатить счёт завтра 18:00         # Завтра в 18:00
/add Позвонить маме                     # Без времени (через 1 час)
```

### Настройки

```bash
/settings                               # Показать текущие настройки
/settings auto_delete 7                 # Удалять выполненные задачи через 7 дней
/settings auto_delete 0                 # Отключить автоудаление
```

## 🗄️ База данных

База данных SQLite (`tasks.db`) создаётся автоматически при первом запуске.

**Структура таблицы `tasks`:**
- `id` — уникальный идентификатор задачи
- `user_id` — ID пользователя Telegram
- `text` — текст задачи
- `datetime` — время напоминания (ISO формат)
- `status` — статус задачи (`pending`/`done`)
- `created_at` — дата создания записи
- `completed_at` — дата завершения задачи

**Структура таблицы `user_settings`:**
- `user_id` — ID пользователя (PRIMARY KEY)
- `auto_delete_days` — количество дней до автоудаления выполненных задач

## ⚙️ Технические детали

- **Фреймворк:** aiogram 3.x
- **База данных:** SQLite (aiosqlite)
- **Парсинг времени:** Регулярные выражения
- **Автоматические напоминания:** Проверка каждые 60 секунд
- **Автоочистка:** Удаление истёкших задач с учётом индивидуальных настроек
- **Graceful shutdown:** Корректное завершение работы при Ctrl+C

## 📝 Особенности

- Если время напоминания не указано, напоминание приходит через 1 час
- Если указанное время уже прошло сегодня, напоминание устанавливается на завтра
- Выполненные задачи автоматически удаляются согласно настройкам пользователя (по умолчанию через 1 день)
- Просроченные задачи (более 24 часов) удаляются автоматически
- Каждый пользователь может настроить время автоудаления выполненных задач индивидуально

## 🔒 Безопасность

- Файл `.env` с токеном исключён из Git (см. `.gitignore`)
- Не публикуйте токен бота в открытом доступе
- Используйте `.env.example` как шаблон

## 📄 Лицензия

Этот проект является частью портфолио. Используйте свободно для обучения и разработки.

## 👤 Автор

SpiritWalker84

---

**Часть портфолио:** [telegram-bots-portfolio](https://github.com/SpiritWalker84/telegram-bots-portfolio)
